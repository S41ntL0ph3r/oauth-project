# ⚠️  ARQUIVO DE EXEMPLO - NÃO USAR EM PRODUÇÃO!
# Este arquivo serve apenas como template. Configure as variáveis reais no Vercel ou .env.local

# =============================================================================
# NEXTAUTH CONFIGURAÇÃO (OBRIGATÓRIO)
# =============================================================================
AUTH_SECRET="ALTERAR_CHAVE_SECRETA_SUPER_SEGURA_64_CARACTERES_MINIMO"
NEXTAUTH_URL="https://seu-dominio-producao.vercel.app"

# =============================================================================
# ADMIN PANEL CONFIGURAÇÃO (OBRIGATÓRIO)
# =============================================================================
ADMIN_JWT_SECRET="ADMIN_JWT_SECRET_SUPER_SEGURA_64_CARACTERES_MINIMO_PARA_PAINEL"

# =============================================================================
# GITHUB OAUTH (Configure no GitHub Developer Settings)
# =============================================================================
AUTH_GITHUB_ID="Gh1thub_Cl1ent_ID_Ex4mple"  
AUTH_GITHUB_SECRET="github_cl1ent_s3cr3t_exemplo_nao_usar"

# =============================================================================
# DATABASE (SENSÍVEL - USE URLs SEGURAS)
# =============================================================================
# Para desenvolvimento local (SQLite)
DATABASE_URL="file:./dev.db"

# Para produção (PostgreSQL/Neon/PlanetScale) - EXEMPLO:
# DATABASE_URL="postgresql://usuario:senha@host.neon.db:5432/banco_producao?sslmode=require"

# =============================================================================
# EMAILJS CONFIGURAÇÃO (PÚBLICO - mas substitua os valores)
# =============================================================================
NEXT_PUBLIC_EMAILJS_SERVICE_ID="service_exemplo123"
NEXT_PUBLIC_EMAILJS_TEMPLATE_RESET_PASSWORD="template_reset_123"
NEXT_PUBLIC_EMAILJS_TEMPLATE_VERIFY_EMAIL="template_verify_123" 
NEXT_PUBLIC_EMAILJS_PUBLIC_KEY="sua_chave_publica_emailjs"

# =============================================================================
# OPCIONAIS - CONFIGURAÇÕES ADICIONAIS
# =============================================================================
# Para uploads de arquivo
UPLOAD_MAX_SIZE="5242880"  # 5MB em bytes
UPLOAD_ALLOWED_TYPES="image/jpeg,image/png,image/webp"

# Para rate limiting
RATE_LIMIT_MAX="100"      # Máx requests por janela
RATE_LIMIT_WINDOW="3600"  # Janela em segundos (1 hora)

# Para logs e debugging (não usar 'true' em produção)
DEBUG_MODE="false"
LOG_LEVEL="error"

# =============================================================================
# INSTRUÇÕES DE SEGURANÇA:
# =============================================================================
# 1. NUNCA commitar o arquivo .env real
# 2. Use senhas fortes com pelo menos 32 caracteres
# 3. Configure estas variáveis diretamente no Vercel Dashboard
# 4. Use diferentes valores para desenvolvimento e produção
# 5. Rotacione secrets regularmente
# =============================================================================
